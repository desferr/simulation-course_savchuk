### Лабораторная работа №1  
**Задача**  
Реализовать приложение для моделирования полёта тела в атмосфере.  
Предусмотреть возможность ввода шага моделирования и вывода результатов.  
Приложение было написано на языке *C#* в *Visual Studio* с помощью *WinForms*.  
Для моделирования были использованы следующие формулы:  
x = v0 * cos(a) * t  
y = v0 * sin(a) * t - ((g * t ^ 2) / 2)  
vx = v0 * cos(a)  
vy = v0 * sin(a) - g * t  
v = sqrt(vx ^ 2 + vy ^ 2)  
**Пример работы приложения**  
Интерфейс приложения после запуска одной симуляции:  
![interface_example.png](./pictures/interface_example.png)  
Проведём 5 симуляций с одинаковыми для каждой параметрами стартовых скорости (*5 м/c*) и угла (*60°*),  
но каждая симуляция будет иметь на порядок меньший шаг моделирования, начиная *1 с* и заканчивая *0,0001 с*.  
Картинка, показывающая траектории симуляций:  
![all_trajectories.png](./pictures/all_trajectories.png)  
Таблица, отображающая результаты каждой симуляции:
| Шаг моделирования, с | 1 | 0.1 | 0.01 | 0.001 | 0.0001 |
|----------------------|---|-----|------|-------|--------|
| Дальность полёта, м |2,5|2,25|2,225|2,21|2,209|
| Максимальная высота, м |0|0,948|0,957|0,957|0,957|
| Скорость в конечной точке, м/с |6,014|5,139|5,054|5,003|5|

**Вывод**  
При решении задач, связанных с имитационным моделированием, очень важно правильно подбирать  
шаг моделирования. При слишком маленьком шаге моделирования точность результатов может быть неудовлетворительной,  
но при увеличении шага в определённый момент прирост точности результатов становится слишком маленьким,  
чтобы оправдывать дополнительные временные затраты.
